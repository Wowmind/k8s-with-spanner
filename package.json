{
  "name": "cloud-spanner-gke-sample",
  "version": "1.0.0",
  "description": "Sample application that uses Cloud Spanner with GKE",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/server.js",
    "build": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@google-cloud/spanner": "^7.0.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
